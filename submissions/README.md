# Submissions index

Active ladder pair and parked research. Upload artifacts are the `.tar.gz` files (include `cg/` engine). Source `main.py` / `deck.csv` are copied here for review without committing full engine trees.

| Folder | Artifact | Role | Notes / signal | Local origin |
|--------|----------|------|----------------|--------------|
| `day2_alakazam_v12/` | `Day2_alakazam_v12.tar.gz` | **Day-2 (active)** | Plain v12; ladder/eval ~812 vs bossguard ~659 | `outputs/submission_candidates/` |
| `day1_lucario/` | `DAY1_mega_lucario_rule_core.tar.gz` | **Day-1 (active)** | SHA matches DAY1 Lucario; see folder README | `outputs/submission_candidates/` |
| `bossguard_parked/` | `alakazam_v12_code_deck_soft_bossguard.tar.gz` | Parked research | Offline ~13/15 tagged ≠ ladder; do not ship without remine | `outputs/submission_candidates/` |

## Upload order (accepted pair)

1. `day2_alakazam_v12/Day2_alakazam_v12.tar.gz`
2. `day1_lucario/DAY1_mega_lucario_rule_core.tar.gz`

## Dates

- Writeup / slot-pollution fix notes: **2026-07-26** (see `../writeup/strategy_insights.md`)

## What is not here

Full `cg/` binary trees (~5 MB extracted each) live inside the tarballs. Large episode dumps stay under `outputs/kaggle_episodes/` (gitignored).
